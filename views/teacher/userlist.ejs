<!DOCTYPE html>
<html>
<head>
	<title>User List</title>
</head>
<body>
	<h1>User List</h1>

	<a href="/teacher">Back</a> |
	<a href="/logout">logout</a> 
	<br>
	<br>

	<table border="1" width="400px" >
		<tr>
			<th>ID</th>
			<th>PASSWORD</th>
			<th>TYPE</th>
			<th>Action</th>
		</tr>

		<% for(var i=0; i < uList.length; i++){ %>
		<tr>
			<td><%= uList[i].U_ID %></td>
			<td><%= uList[i].U_PASSWORD %></td>
			<td><%= uList[i].U_TYPE %></td>
			<td>
				<a href="/teacher/edit/<%= uList[i].id %>">Edit</a> |
				<a href="/teacher/delete/<%= uList[i].id %>">Delete</a> 
			</td>
		</tr>
		<% } %>
	</table>
</body>
</html>